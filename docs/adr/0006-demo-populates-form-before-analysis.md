# Demo populates form before analysis

The "Try the demo scenario" button populates the form fields with demo values instead of bypassing them directly to analysis. This lets users see the scenario description and regulatory question before the answer appears, making the demo more transparent and allowing them to edit the values if desired. The same scenario (identified by description-derived id) can have multiple questions, so editing the question produces a new analysis of the same scenario.
