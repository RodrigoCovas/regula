# Backend URL for Next.js /api/* rewrites.
# In dev mode the backend runs on localhost:8000.
# In Docker Compose this points at the backend service: http://backend:8000
BACKEND_URL=http://localhost:8000
