DATABASE_URL=postgresql://regula:regula@localhost:5432/regula
OLLAMA_API_URL=http://localhost:11434
OPENROUTER_API_KEY=your_openrouter_key_here
# Note: the OpenRouter model is pinned to deepseek/deepseek-v4-flash:free in
# backend/src/config.py. There is deliberately no env-var override to a paid model.
